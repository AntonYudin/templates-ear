<?xml version="1.0" encoding="UTF-8"?>
<resources>
  <Resource id="datasources/default-datasource" type="javax.sql.DataSource">
    JdbcDriver = org.mariadb.jdbc.Driver
    JdbcUrl    = ${jdbc.mariadb.url}
    UserName   = ${jdbc.mariadb.user}
    Password   = ${jdbc.mariadb.password}
    defaultAutoCommit = true
    testOnReturn = true
    testWhileIdle = true
    timeBetweenEvictionRunsMillis = 60
    initialSize = 2
    minIdle = 2
    validationQuery = select 1
  </Resource>

</resources>

